name: V09 Current CMC 101-300
on:
  push:
    branches: [chatgpt-v09-data-bridge-20260917]
  workflow_dispatch:
jobs:
  collect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 v09_bridge/cmc_now.py
      - uses: actions/upload-artifact@v4
        with:
          name: v09-cmc-now
          path: v09_cmc_now/
          retention-days: 1
